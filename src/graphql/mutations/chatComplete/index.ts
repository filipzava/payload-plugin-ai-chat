// todo
/*
// plugin.ts

 graphQL: {
        ...config.graphQL,
        mutations: (GraphQL, payload) => ({
          ...config.graphQL?.mutations,
          chatComplete: chatCompleteMutation(GraphQL, payload, pluginConfig),
        }),
      },

*/
